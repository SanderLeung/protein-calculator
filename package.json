{
  "name": "protein-calculator",
  "version": "1.0.0",
  "description": "",
  "main": "grocery-list.js",
  "scripts": {
    "pretest": "NODE_ENV=test sequelize db:migrate:undo:all && NODE_ENV=test sequelize db:drop && NODE_ENV=test sequelize db:create && NODE_ENV=test sequelize db:migrate",
    "test": "cross-env NODE_ENV=test jest --testTimeout=5000 --collectCoverage",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "sequelize": "^6.35.2",
    "sequelize-cli": "^6.6.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "supertest": "^6.3.4"
  }
}
